import React from 'react'
import { Grid } from 'semantic-ui-react'

const SiteListDash = (props) => {
	// const sites = props.sites.map(site => <li> site.base_url </li>)
	return (
	<Grid.Column width={8}>
		
	</Grid.Column>
	)
}

export default SiteListDash